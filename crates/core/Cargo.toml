[package]
name = "simple-archiver-core"
version = "0.1.0"
edition = "2021"

[dependencies]
loom = { version = "0.7", optional = true }

[dev-dependencies]
mockall = "0.13"

[features]
loom = ["dep:loom"]
